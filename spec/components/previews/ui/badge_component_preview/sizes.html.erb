<%# filepath: /home/baloz/uV/side-projects/Collab/go3/spec/components/previews/ui/badge_component_preview/sizes.html.erb %>
<div class="space-y-6 p-4">
  <h2 class="text-xl font-bold">Badge Sizes</h2>
  
  <div class="flex flex-wrap items-end gap-4">
    <div class="flex flex-col items-center">
      <p class="text-sm font-medium mb-2">Extra Small (xs)</p>
      <%= render(Ui::BadgeComponent.new(size: :xs)) do %>
        Extra Small
      <% end %>
    </div>
    
    <div class="flex flex-col items-center">
      <p class="text-sm font-medium mb-2">Small (sm)</p>
      <%= render(Ui::BadgeComponent.new(size: :sm)) do %>
        Small
      <% end %>
    </div>
    
    <div class="flex flex-col items-center">
      <p class="text-sm font-medium mb-2">Medium (md)</p>
      <%= render(Ui::BadgeComponent.new(size: :md)) do %>
        Medium
      <% end %>
    </div>
    
    <div class="flex flex-col items-center">
      <p class="text-sm font-medium mb-2">Large (lg)</p>
      <%= render(Ui::BadgeComponent.new(size: :lg)) do %>
        Large
      <% end %>
    </div>
  </div>
</div>
