<div class="preview-container" style="width: 100%; height: 400px; border: 1px solid #e5e7eb; overflow: hidden;">
  <h2 class="text-lg font-semibold mb-4">Tab Actions Menu Example</h2>
  <p class="mb-4 text-gray-700">This example demonstrates the tab actions menu that allows users to manage multiple tabs at once. Click the three dots (⋮) at the right of the tab bar to access the tab actions menu.</p>
  
  <%= render Navigation::PageTabsComponent.new(current_path: "/organizations/1") do |c| %>
    <%= c.with_tab(title: "Dashboard", path: "/dashboard", active: false) %>
    <%= c.with_tab(title: "Organizations", path: "/organizations", active: false) %>
    <%= c.with_tab(title: "Sample Organization", path: "/organizations/1", active: true) %>
    <%= c.with_tab(title: "User Profile", path: "/users/profile", active: false) %>
    <%= c.with_tab(title: "Settings", path: "/users/settings/edit", active: false) %>
  <% end %>
</div>

<div class="mt-6 bg-gray-50 p-4 rounded-lg">
  <h3 class="text-md font-medium mb-2">Usage:</h3>
  <pre class="text-sm bg-gray-800 text-white p-4 rounded"><code>
  <%= render Navigation::PageTabsComponent.new(current_path: "/organizations/1") do |c| %>
    <%= c.with_tab(title: "Dashboard", path: "/dashboard", active: false) %>
    <%= c.with_tab(title: "Organizations", path: "/organizations", active: false) %>
    <%= c.with_tab(title: "Sample Organization", path: "/organizations/1", active: true) %>
    <%= c.with_tab(title: "User Profile", path: "/users/profile", active: false) %>
    <%= c.with_tab(title: "Settings", path: "/users/settings/edit", active: false) %>
  <% end %>
  </code></pre>
</div>

<div class="mt-6 p-4 rounded-lg border border-red-200 bg-red-50">
  <div class="flex items-start">
    <div class="flex-shrink-0">
      <svg class="h-5 w-5 text-red-400" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
        <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2h-1V9z" clip-rule="evenodd" />
      </svg>
    </div>
    <div class="ml-3">
      <h3 class="text-sm font-medium text-red-800">Available Tab Actions</h3>
      <ul class="mt-2 text-sm text-red-700 list-disc pl-5">
        <li><strong>Close All Tabs</strong> - Closes all tabs and returns to the home page</li>
        <li><strong>Close Other Tabs</strong> - Closes all tabs except the currently active one</li>
        <li><strong>Close Tabs to the Right</strong> - Closes all tabs to the right of the active tab</li>
        <li><strong>Close Tabs to the Left</strong> - Closes all tabs to the left of the active tab</li>
      </ul>
    </div>
  </div>
</div>

<div class="mt-6 p-4 rounded-lg border border-orange-200 bg-orange-50">
  <div class="flex items-start">
    <div class="flex-shrink-0">
      <svg class="h-5 w-5 text-orange-400" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
        <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2h-1V9z" clip-rule="evenodd" />
      </svg>
    </div>
    <div class="ml-3">
      <h3 class="text-sm font-medium text-orange-800">How to Test</h3>
      <ol class="mt-2 text-sm text-orange-700 list-decimal pl-5">
        <li>Click the ellipsis (⋮) button to the right of the tab bar</li>
        <li>Select an action from the dropdown menu</li>
        <li>See how the tabs respond to your selected action</li>
      </ol>
    </div>
  </div>
</div> 