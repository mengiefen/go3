<div class="preview-container" style="width: 100%; height: 400px; border: 1px solid #e5e7eb; overflow: hidden;">
  <h2 class="text-lg font-semibold mb-4">Non-Closable Tabs Example</h2>
  <p class="mb-4 text-gray-700">This example shows a mix of closable and non-closable tabs. Non-closable tabs don't have the close (×) button.</p>
  
  <!-- Simulated Tab Bar for Preview -->
  <div class="bg-white border border-gray-200 rounded-md overflow-hidden">
    <div style="height: 48px; display: flex; align-items: center; background-color: #f9fafb; border-bottom: 1px solid #e5e7eb; padding: 0 0.5rem;">
      <div style="display: flex; flex-grow: 1; gap: 0.25rem; overflow-x: auto; align-items: center;">
        <!-- Non-closable tab (active) -->
        <div style="display: flex; align-items: center; height: 36px; padding: 0 12px; background-color: white; border-radius: 0.25rem; color: #3b82f6; font-weight: 500; border: 1px solid #e5e7eb; white-space: nowrap;">
          <span>Dashboard</span>
          <!-- No close button for non-closable tab -->
        </div>
        <!-- Regular closable tab (inactive) -->
        <div style="display: flex; align-items: center; height: 36px; padding: 0 12px; background-color: #f3f4f6; border-radius: 0.25rem; color: #6b7280; border: 1px solid transparent; white-space: nowrap;">
          <span>Organizations</span>
          <button style="display: flex; align-items: center; justify-content: center; margin-left: 8px; width: 20px; height: 20px; border-radius: 50%; color: #9ca3af;">
            <i class="fas fa-times" style="font-size: 12px;"></i>
          </button>
        </div>
      </div>
      <div style="display: flex; align-items: center;">
        <button style="display: flex; align-items: center; justify-content: center; width: 32px; height: 32px; border-radius: 0.25rem; color: #6b7280;">
          <i class="fas fa-ellipsis-v"></i>
        </button>
      </div>
    </div>
    <div style="padding: 16px;">
      <p>Content of the Dashboard tab would be loaded here.</p>
    </div>
  </div>
</div>

<div class="mt-6 bg-gray-50 p-4 rounded-lg">
  <h3 class="text-md font-medium mb-2">Usage:</h3>
  <pre class="text-sm bg-gray-800 text-white p-4 rounded"><code>
  <%= render Navigation::PageTabsComponent.new(current_path: "/dashboard") do |c| %>
    <%= c.with_tab(title: "Dashboard", path: "/dashboard", active: true, closable: false) %>
    <%= c.with_tab(title: "Organizations", path: "/organizations", active: false, closable: true) %>
  <% end %>
  </code></pre>
</div>

<div class="mt-6 p-4 rounded-lg border border-green-200 bg-green-50">
  <div class="flex items-start">
    <div class="flex-shrink-0">
      <svg class="h-5 w-5 text-green-400" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
        <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2h-1V9z" clip-rule="evenodd" />
      </svg>
    </div>
    <div class="ml-3">
      <h3 class="text-sm font-medium text-green-800">Use Case</h3>
      <p class="mt-2 text-sm text-green-700">
        Non-closable tabs are useful for keeping important pages (like the Dashboard or Home) always open and accessible, while still allowing users to close other tabs they've opened.
      </p>
    </div>
  </div>
</div> 