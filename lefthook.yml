pre-commit:
  parallel: true
  commands:
    rubocop:
      glob: "*.rb"
      exclude: "db/schema.rb"
      run: rubocop {staged_files}
    eslint:
      glob: "app/javascript/**/*.js"
      run: yarn eslint {staged_files}