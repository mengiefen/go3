export enum TagName {
  Auth = 'Auth',
  Result = 'Result',
  Registry = 'Registry',
  Datasets = 'Datasets',
}

export const tagNames: TagName[] = [
  TagName.Auth,
  TagName.Result,
  TagName.Registry,
  TagName.Datasets,
];
